server:
  port: 8090

application:
  kafka:
    topic: "demo-topic"


spring:
  kafka:
    consumer:
      group-id: "test-group"
      bootstrap-servers: "127.0.0.1:9092"
      client-id: "demo-consumer"
      auto-offset-reset: earliest